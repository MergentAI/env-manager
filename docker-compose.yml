version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    restart: unless-stopped
    # Expose internally to other containers
    expose:
      - "3000"
    volumes:
      - ./packages/server/data:/app/packages/server/data
    environment:
      - PORT=3000
      - ADMIN_SECRET=${ADMIN_SECRET}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  client:
    build:
      context: .
      dockerfile: packages/client/Dockerfile
    restart: unless-stopped
    # Expose internally to other containers / proxy
    expose:
      - "80"
    depends_on:
      server:
        condition: service_healthy
